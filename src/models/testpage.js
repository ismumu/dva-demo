
export default {
	namespace: 'testpage',
	state: {
		text: 'aaaaaa',
	},

	subscriptions: {
		// setup({ dispatch, history }) {},
	},

	effects: {
		// *fetch({ payload }, { call, put }) {
		// 	yield put({ type: 'save' });
		// },
	},

	reducers: {
		// save(state, action) {
		// 	return { ...state, ...action.payload };
		// },
	},

};
